# HIHO MOCAP — Live Status

One glance, any machine, any session. Update at the end of every dev session (laptop is ground truth).

- **Canonical build:** `SOFTWARE/hiho_mocap-1.4.38.zip` — **LAPTOP-VERIFIED 2026-08-05 on final Blender 5.2.0 LTS (laptop swapped off beta same morning): full artist path Load Take → Spawn → Bake on the REFILTERED60 perimeter walk; bake's continuity pass left 0 sign flips across 63 bones × 3600 keys (independent oracle scan); David walked the smoothed result — "best take yet", candidate demo file; artifact `HIHO_TESTING&CALIBRATION_BLENDS/HIHO_NEWRING_TESTWALK2_REFILTERED60_1438_BUILDTEST.blend`.** BUILD DAY 2026-08-04 (desktop, explicit machine-roles override; laptop's noon session paused at zero changes and is superseded — do NOT resume it). Six builds 1.4.33→1.4.38, one change + one zip each, every change tested against the real 08-01 perimeter-walk take on the desktop before the next began. Face chain last live-verified at BASEMENT on 1.4.32 (07-22); nothing in 1.4.33–38 touches the face path. Top level holds 1.4.38 + 1.4.37; older zips in `ARCHIVES/OLD_ZIPS/`.
- **2026-08-04 BUILD DAY, what shipped:** **1.4.33** bake now runs the quaternion continuity pass (Z-snap-under-smoothing fixed at the source; on the perimeter walk it re-spelled 15,114 keys on exactly the 13 bones the diagnosis predicted; pose-neutral to 1e-6; report says what it did). **1.4.34** position pre-filter clocked from the take's real fps (was hardcoded 30 while takes are 60 — every take arrived half-filtered; panel log now prints the rate). **1.4.35** blank Cameras box refuses with "Click Show Cameras first" instead of silently recording 0,1,2,3; camera_ids default now blank; `HIHO_Record.command` does the six-camera ring, takes cams+seconds as arguments, and no longer prints "Done" on failure. **1.4.36** Bake and Save Out filter on `HIHO_` constraints only — a character's own rigging survives baking (CloudRig-safe). **1.4.37** helper crash text: last 20 lines attached to the panel error + printed to console; `errors="replace"` so one bad byte can't kill the watcher. **1.4.38** Process clears the stale quality badge on start and stamps the take name on it; runner env guard is `is_file` (blank/folder path now a friendly error, not a raw traceback). Plus: **AGPL-3.0 LICENSE** at repo root + in every zip; **`build_zip.py`** (reproducible zips, honors `# RETIRED` headers; the six stray docs the hand-zips carried — including the stale self-misidentifying STATUS.md — no longer ship). Desktop test env: `~/freemocap-env` (uv, py3.11, freemocap 1.8.2).
- **RING REMOUNTED 2026-08-01: the three-band layout is live and it wins.** ~270° wrap, 2 high (≈1.8 m) / 2 mid (1.24–1.44 m) / 2 low (0.87–1.02 m), open sector southeast. Calibration `2026-08-01_17-16-40`: **EXCELLENT 0.42 px — all-time best** (0.45 on 7/31, 0.56 on 7/22). **Clean volume radius ~1.00 m, up from 0.75 m** — and it's a data horizon (jitter never crossed threshold out to where data ended), so possibly more; next mapping take should push past 1 m, especially east/southeast. Cameras ~1.5–2 m out; "push back in steps" headroom remains.
- **HARD RULE (learned 2026-08-01 the hard way): camera hubs carry CAMERAS ONLY, max 2 per hub.** A keyboard+mouse on one camera hub starved that camera to ~5 fps (normal-looking image!), which stalls recording forever — the recorder counts frames, not the clock. Three failed calibrations + a reboot before David found it. Full trace + diagnosis recipe: BUGS_AND_BACKLOG.md 2026-08-01.
- **Helmet A/B (07-22): APPROVED** — commercial **Face Mocap Headrig**, no head-landmark penalty. **Clamp rule (07-31): pads grip BEZEL, never glass** — clamp on the screen pressed the record button → ~20 micro-takes of 1–5 s. Before a real take: 15 s clamped dry-run + glance the take-tile duration before AirDrop.
- **Capture protocol v2:** calibrate EVERY session, 200 mm board, **square = 200 — the field defaults to 110 in every fresh .blend, re-check it** (bit twice on 08-01; default-fix queued). Both badges green and named. Blink the sync light while STANDING STILL (blink frames sacrificial; mark FIRST dip both sides). iPhone in **Live Link (ARKit) mode**, never MetaHuman Animator. LLF ZIP → unzip into `~/Desktop/HIHO_FACE_TAKES/`; Load Face Take wants the `_cal.csv`. Recording stop key is **ESC (Q does nothing — fix queued)**.
- **2026-08-04 AUDIT DAY (laptop, no code touched — three doc sets landed):** (1) **The 1-frame Z-snap-under-smoothing mystery is SOLVED** — quaternion sign flips manufactured by our own bake (`bake_animation.py:59`); hits every take where the performer turns past 180°; Blender's Euler Filter can't fix quaternions. Repair script validated + **David live-verified on the spin-heavy 08-01 perimeter walk ("best yet")**. **Interim rule: after every Bake, run `HIHO_TESTING&CALIBRATION_BLENDS/HIHO_quaternion_continuity_repair.py` BEFORE any smoothing.** Full story: `Z_JITTER_DIAGNOSIS_2026-08-04.md`. (2) **Every take is pre-filtered at the wrong frame rate** — FreeMoCap's position pre-filter assumes 30 fps, ours is 60, so takes arrive half-filtered (effective ~14 Hz cutoff vs intended 7). One-line fix in `external/process_take.py`. (3) Pre-semester audit: `AUDIT_2026-08-04.md`. Per-region smoothing validated + researched: `SMOOTHING_METHODOLOGY_2026-08-04.md` + `SMOOTHING_RESEARCH_PRIOR_ART_2026-08-04.md` (never smooth root travel; strength = two dials).
- **BUILD DAY QUEUE: DONE 2026-08-04 (all six + LICENSE + build script — see "what shipped" above). Git commit: this session.** Still queued: **A** recording hard-stops at Length + honest per-camera frame report → **C** honor Q (bundle with A) → **B** Camera Health preflight button (3 s per-camera fps meter) → square-size default 200 → panel-Stop signal handling (audit #3) → relative links in processed takes (audit #6) → face frame-rate warning (audit #7) → smaller items in audit #9. Details: BUGS_AND_BACKLOG.md 2026-08-01 #1–2 + AUDIT_2026-08-04.md. **Untestable off-ring today (verify next ring session): a real recording through the new blank-box guard + launcher.**
- **UI PRUNE/ADD QUEUE — BLESSED by David 2026-08-05 (laptop UI review of 1.4.38), builds started same session:** one change + one zip each: (1) **1.4.39** square-size default 110→200 + fix the field's help text (still calls 110 the house board) → (2) **1.4.40** panel cleanup in one build: wire Show Science Mode for real (it was a dead switch — nothing read it; gate the technical panel behind it), hide the stubbed Polish section (Fix-wrist-flips is obsolete — bake self-repairs now; Smooth-hands label predates per-region research), hide "Pick from library..." (v2.0) → (3) **1.4.41** make Preview "Play" real (wire to animation play; retire the stub operator) → (4) **1.4.42** capture save-location becomes an AddonPreferences path ("HIHO data home", default `~/Desktop`; David points his at `HIHO_TESTING&CALIBRATION_BLENDS` — closes the 08-02 hardcoded-paths note; touches `HIHO_Record.command` + `HIHO_LAST_CAPTURE.txt` write + `external_capture.py` + `calibration.py` together) → (5) panel-Stop signal fix rides with recorder A/C/B. Two-panel split (Studio vs technical) stays as designed.
- **GitHub publish (2026-08-05, laptop): HISTORY CLEANED + account confirmed.** `git filter-repo` stripped 7 third-party R&D files from all history (two conference-talk mp4s incl. the 126 MB GitHub-blocker, their transcripts, a saved blog PDF, foscap zip, okgo demo blend; David's own PUPPET_SHOW doc kept; files restored to disk untracked; snapshot in `ARCHIVES/GIT_BACKUP_PRE_FILTER_2026-08-05/`). 105 commits before and after; **commit ids changed** (build-day story now `51bdb6e…60c00c1`). David's GitHub account: **davidbayus** (Google sign-in linked Nov 2025). Plan: private repo `hiho-software` first, public-unannounced later after his skim (student-guide PDF filename, credit mentions) + README. gh CLI: install from official release (no brew on laptop), David does the browser login.
- **Pending live checks:** (1) Save Out on the chibi; (2) **body+face take under protocol v2 + clamp rules — rolls to next session, first move**; (3) charging-while-recording (low priority).
- **Next up:** body+face take → **character-binding brainstorm** (prep done — see SESSION_HANDOFF_2026-08-01 "Character-binding prep": Send to Character is ajc27-name-matched, Load Face Take feeds one mesh; three design shapes drafted) → **ARTIST_INTUITION design doc** (1.5 theme, David's 07-22 directive) folding in the 07-22 paper cuts + 08-01 findings. Then: demo reel; paper layer (calibration card + student quickstart); Extensions-format install; GAP_FILL_DESIGN.

*Last updated: 2026-08-05 (laptop — 1.4.38 verified on final LTS via the real panel path, David walked it: "best take yet"; UI queue blessed + building; git history cleaned for GitHub, account confirmed davidbayus; August newsletter poured + saved, awaiting his review pass. Detail: `SESSION_HANDOFF_2026-08-05.md`.)*
